import 'package:flutter/material.dart';

class UserRegistrator extends StatelessWidget {
  const UserRegistrator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
